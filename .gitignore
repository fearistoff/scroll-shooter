node_modules/
dist/
.DS_Store
*.local

# Служебные файлы Claude Code (конфиг dev-сервера, локальные настройки)
.claude/

# Исходные материалы задания: ТЗ, план слоёв и референсные скриншоты.
/*.md
/*.png

# README — документация проекта, он должен быть в репозитории.
# Исключение обязано идти ПОСЛЕ шаблона /*.md: git применяет правила по порядку,
# и выше него оно бы не сработало.
!/README.md

# Временная раскладка для проверки публикации в подпапке (см. README)
dist-pages/
