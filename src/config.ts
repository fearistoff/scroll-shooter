/**
 * Единая точка настройки прототипа. Все числовые параметры игры — здесь.
 * Секции пронумерованы по разделам game-design-doc.md.
 *
 * ЕДИНИЦЫ ИЗМЕРЕНИЯ: 1 unit = 1 метр.
 *   рост человека        1.8
 *   ширина дороги        10
 *   видимая глубина      ~42 (от z=+4 у нижнего края экрана до z=-38 у верхнего)
 *
 * ОРИЕНТАЦИЯ ОСЕЙ (задана в слое 0, дальше не меняется):
 *   z = 0     линия отряда, главный герой
 *   -Z        дорога уходит вдаль; зомби/бочки/ворота спавнятся в z ≈ -40
 *   +Z        камера позади отряда; всё едет на игрока в сторону +Z
 *   x         влево-вправо, дорога от -5 до +5
 *   y         вверх, земля в y = 0
 *
 * Метки TODO — вопросы из раздела 15 ТЗ, значение выбрано нами и требует обкатки.
 */

export const CONFIG = {
  /** Интерфейс поверх сцены. */
  ui: {
    /**
     * Полоски HP над стрелками и всеми зомби кроме босса.
     *
     * ПРАВИЛА ПОКАЗА РАЗНЫЕ, и это намеренно:
     *   стрелки — полоска висит, ПОКА ЗАПАС НЕ ПОЛНЫЙ, без таймера. У них есть
     *     регенерация (player.regen), поэтому полоска гаснет сама, когда HP
     *     отыграется, и всё время до этого игрок видит, кто недолечен;
     *   зомби — только showSeconds после последнего урона. Регенерации у них нет,
     *     то есть по правилу «пока не полное» задетый зомби носил бы полоску до
     *     смерти, а их на дороге до 200 — экран превратился бы в кашу.
     * У босса своя многослойная полоса в HUD, полоска ему не нужна.
     */
    hpBar: {
      /**
       * Сколько ИГРОВЫХ секунд полоска видна после последнего урона.
       * Относится ТОЛЬКО к зомби: у стрелков показ считается по запасу HP.
       */
      showSeconds: 3,
      /**
       * Высота над макушкой цели, units.
       *
       * Не «на глаз»: камера смотрит вниз под 55°, и у линии отряда 0.35 units
       * высоты сжимаются всего в 4 экранных пикселя — полоска ложилась прямо на
       * голову. Замерено проекцией: 0.9 даёт ~10 px зазора у отряда и ~16 px
       * у зомби в глубине (дальние объекты меньше сжаты по вертикали).
       */
      offsetY: 0.9,
      /** Размер в CSS-пикселях: постоянный, как и у чисел-подписей. */
      width: 26,
      height: 4,
      /**
       * Множитель размера полоски ОБЫЧНОГО зомби: 0.7 — на 30% меньше базовой,
       * то есть 18.2 × 2.8 вместо 26 × 4.
       *
       * Зачем отдельно: обычных зомби на дороге до 200, и полоска в полный размер
       * у каждого забивает кадр сильнее, чем сообщает. У крупного зомби и у
       * стрелков размер остаётся базовым — их единицы, и именно их запас важно
       * читать точно.
       *
       * Дробные пиксели оставлены намеренно: округление 2.8 → 3 дало бы по высоте
       * не −30%, а −25%, и «на 30% меньше» перестало бы быть правдой.
       */
      normalZombieScale: 0.7,
    },
    /**
     * Вспышка цели при получении урона. Работает у ВСЕХ, кто теряет HP: героя,
     * союзников, обычных и крупных зомби, босса.
     *
     * Таймер тает по ИГРОВОМУ dt, как и полоски HP, поэтому на экранах результата
     * и прокачки вспышка не догорает на паузе.
     */
    damageFlash: {
      /**
       * Длительность, секунды. 0.25 — 15 кадров при 60 fps, вместо прежних 0.1.
       *
       * Верхняя граница задана не вкусом, а частотой попаданий по цели:
       *   обычный зомби — умирает с первого-третьего попадания в зависимости от
       *     ствола, вспыхнет считаное число раз, длительность на него почти не
       *     влияет;
       *   крупный зомби и босс — получают попадания непрерывно (полный отряд из 23
       *     с пулемётом даёт 345 выстрелов в секунду), поэтому при ЛЮБОЙ длительности
       *     светятся постоянно, пока по ним стреляют;
       *   герой под напором — несколько зомби бьют раз в секунду каждый, так что
       *     при 0.25 он в плотном бою подсвечен большую часть времени.
       * То есть дальше поднимать можно, но вспышка окончательно перестанет читаться
       * как отдельное событие и станет индикатором «по мне сейчас попадают».
       */
      seconds: 0.075,
      /**
       * Насколько цель светлеет: доля пути от собственного цвета к белому.
       *
       * Вспышка — СВЕТЛЫЙ ОТТЕНОК своего цвета, а не общий посторонний тон: зелёный
       * зомби светлеет в бледно-зелёный, коричневый в светло-коричневый, синий
       * герой в голубой. Раньше здесь стоял общий красный, и на миг все становились
       * одинаковыми.
       *
       * ЗНАЧЕНИЕ КАЖЕТСЯ МАЛЫМ, НО ОНО НЕ МАЛОЕ. Смешивание идёт в ЛИНЕЙНОМ
       * пространстве (three переводит цвета туда при создании), а не в sRGB,
       * поэтому 0.2 по линейной шкале — это уже +64…190% яркости на экране.
       *
       * ЗАМЕРЕНО, что получается при разных значениях (цвет вспышки, прирост
       * яркости, насыщенность до → после):
       *   0.2   зомби #9bad91  +64%   0.46→0.20     ← выбрано
       *         герой #8aa4dd  +74%   0.81→0.48
       *         босс  #a18a8a  +190%  0.48→0.17
       *   0.3   зомби #acbaa4  +96%   0.46→0.14
       *   0.65  зомби #dbe0d8  +209%  0.46→0.11     почти серо-белый
       * С 0.3 собственный цвет уже теряется, и «светлый оттенок» превращается в
       * общую белую вспышку — то есть в ту же обезличенность, от которой уходили.
       *
       * СЛАБОЕ МЕСТО — СОЮЗНИКИ: 0xd8d2c4 светлеет лишь на +11% (при 0.65 — на 35%),
       * потому что они и так почти белые, а потолок вспышки именно белый: цвет
       * инстанса three УМНОЖАЕТ на цвет материала, умножение только гасит. Заметнее
       * их вспышку можно сделать, только притемнив базовый player.colors.ally.
       */
      lighten: 0.2,
    },
  },

  /** Портретный холст под телефон. */
  viewport: {
    /** Диапазон соотношений сторон (w/h) реальных телефонов: 19.5:9 … 16:9. */
    minAspect: 0.46,
    maxAspect: 0.62,
    /** Ограничение плотности пикселей — перф на мобиле. */
    maxPixelRatio: 2,
    /**
     * Ширина холста в CSS-пикселях, ниже которой HUD переключается в узкую
     * раскладку: полоса волны и номер волны опускаются под верхнюю строку.
     *
     * 460 — не «на глаз», а сумма того, что в верхней строке стоит рядом:
     * отступы 16+16, плашка EXP до ~130 (пятизначный опыт), полоса волны 190,
     * секундомер ~70, зазоры 2×8. Ровно 452, поэтому порог 460 с небольшим
     * запасом. На iPhone (холст 393 px) это включается всегда.
     */
    narrowWidthPx: 460,
  },

  /** Game loop с фиксированным шагом обновления. */
  loop: {
    /** Шаг логики: 60 обновлений в секунду. */
    fixedDt: 1 / 60,
    /** Отсечка большого реального кадра (возврат из фоновой вкладки). */
    maxFrameTime: 0.25,
    /** Максимум шагов логики за кадр — защита от spiral of death. */
    maxStepsPerFrame: 5,
  },

  /**
   * Управление (ТЗ раздел 4). Всё в процентах, чтобы не зависеть от разрешения:
   * положение пальца — % ширины холста, положение отряда — % его хода.
   */
  input: {
    /**
     * Мёртвая зона по краям холста, % ширины с КАЖДОЙ стороны.
     * Палец в этой полосе — отряд уже стоит в крайнем положении и дальше не едет.
     *
     * Зачем: без неё крайнее положение отряда достигается только когда палец
     * упёрся в самый край экрана, а там системные жесты (свайп «назад» в iOS,
     * навигация Android) перехватывают касание. С мёртвой зоной край дороги
     * берётся, не доводя палец до безеля.
     *
     * Заодно определяет, насколько точно отряд стоит под пальцем: при 10 он
     * совпадает с пальцем на телефоне 19.5:9, при ~20 — на 16:9. Промежуточные
     * значения дают лёгкое расхождение у краёв, в центре его нет никогда.
     * Осмысленный диапазон 0…49; выше отряд станет переключаться рывком.
     */
    deadZonePercent: 15,
    /**
     * Курсорные устройства ведут отряд простым наведением, без зажатой кнопки.
     * Тач при этом по-прежнему требует касания — палец не «наводится».
     *
     * Различаем по event.pointerType, а не по медиа-запросу (hover: hover):
     * на ноутбуке с тачскрином оба способа работают одновременно, мышь наведением
     * и палец касанием, вместо выбора одного режима на всё устройство.
     */
    followCursorWithoutPress: true,
  },

  /**
   * Камера сверху-сзади (ТЗ раздел 3).
   * Значения посчитаны под портрет 0.46: отряд попадает на ~20% от низа экрана,
   * видимая глубина дороги ≈ 42 units, дорога ±5 влезает по ширине с обочиной.
   */
  camera: {
    fov: 65,
    near: 0.1,
    far: 200,
    position: { x: 0, y: 20, z: 5 },
    lookAt: { x: 0, y: 0, z: -9 },
    /** Туман по дальнему краю, чтобы объекты не «выпрыгивали» на горизонте. */
    fogNear: 32,
    fogFar: 52,
    fogColor: 0x14161c,
  },

  /** Мир и его движение (ТЗ раздел 3). */
  world: {
    /** Скорость «наезда» мира на отряд, units/сек. От верха экрана до отряда ≈ 7 с. */
    worldSpeed: 6,
    /** Ширина проезжей части: от -5 до +5. */
    roadWidth: 10,
    /** Длина полотна дороги — с запасом за верхний край кадра. */
    roadLength: 120,
    /**
     * Земля по сторонам дороги. Намеренно шире кадра: при 30 units граница земли
     * уезжает за край экрана даже у горизонта, иначе в верхних углах проступает
     * фон (проверено — при 8 units шов виден на широком окне).
     */
    shoulderWidth: 30,
    /** Линия спавна. Верхний край кадра — z = -43.3, спавним чуть за ним. */
    spawnZ: -46,
    /** За этой линией (позади камеры) объекты уходят в пул. */
    despawnZ: 12,
    /** Центральная разметка. */
    markings: {
      dashLength: 2.4,
      dashWidth: 0.22,
      /** Расстояние между началами штрихов. */
      dashSpacing: 6,
      color: 0xb8b2a0,
    },
    /**
     * Придорожные столбики. Одного пунктира по центру мало: у краёв кадра
     * ничего не едет и движение читается слабо. Едут с тем же offset'ом.
     */
    roadside: {
      /** Столбиков на каждой стороне. */
      countPerSide: 12,
      spacingZ: 6,
      size: { x: 0.28, y: 1.0, z: 0.28 },
      /** Смещение от центра — чуть за краем дороги (дорога до ±5). */
      offsetX: 5.9,
      color: 0x9a8f72,
    },
    colors: {
      road: 0x3a3833,
      shoulder: 0x4a3f2e,
    },
  },

  /** Свет. Тени включим, когда появятся модели. */
  lights: {
    ambientColor: 0xbfd0ff,
    ambientIntensity: 1.8,
    dirColor: 0xfff2d8,
    dirIntensity: 2.8,
    dirPosition: { x: -8, y: 24, z: 6 },
  },

  /** Отряд стрелков (ТЗ раздел 5). Реализация — слои 1, 2, 5. */
  player: {
    /** HP главного героя. */
    heroHp: 100,
    /** HP доп. стрелка. */
    allyHp: 30,
    /**
     * Автоматическая регенерация HP стрелков: hpPerInterval за intervalSeconds.
     *
     * Сейчас 1 hp за 2 секунды, то есть 0.5 hp/сек, но лечение начинается лишь
     * через delayAfterDamageSeconds после последнего попадания.
     *
     * Начисляется НЕПРЕРЫВНО (hpPerInterval / intervalSeconds за секунду), а не
     * пульсом раз в intervalSeconds. Средняя скорость та же, но у пульса исход боя
     * зависел бы от того, попал удар зомби за миллисекунду до тика или после, —
     * игрок такую случайность не может ни увидеть, ни отыграть. Непрерывное
     * начисление к тому же видно на полоске HP: у стрелков она висит, пока запас
     * не полный, то есть весь откат и всё лечение видны целиком.
     *
     * ЗАДЕРЖКА ПОСЛЕ УРОНА (delayAfterDamageSeconds) отсчитывается у КАЖДОГО
     * стрелка своя и сбрасывается на полную при любом новом попадании. Смысл в
     * том, чтобы лечение не работало прямо в бою: под напором зомби бьют чаще,
     * чем раз в 10 секунд, задержка не истекает ни разу, и весь отыгрыш
     * достаётся паузам между стычками. Без задержки скорость 0.5 hp/сек просто
     * вычиталась бы из входящего урона в каждом бою.
     *
     * Действует одинаково на героя (потолок heroHp) и на каждого союзника
     * (потолок allyHp) — по ТЗ бонус относится к стрелкам, а не только к герою.
     *
     * ЗАМЕРЕНО, куда на деле уходит урон (10 прогонов по 90 с, перехват воронок
     * hurtHero/hurtAlly): весь урон получает ГЕРОЙ — 8.2 удара и 54 hp за прогон
     * против РОВНО НУЛЯ попаданий по союзникам. Причина в строе: союзники стоят
     * позади героя по +Z, а damageNearestShooter выбирает ближайшего к зомби, и
     * это всегда герой. Союзники вообще получают урон только от босса
     * (damageShootersInCircle и damageAllShooters), поэтому за пределами
     * боссфайта потолок allyHp здесь ни на что не влияет.
     *
     * Замер эффекта на героя (13.9 удара и 88.5 hp вместо 8.2 и 54) делался при
     * скорости 1 hp/сек БЕЗ задержки и к текущим числам НЕ ОТНОСИТСЯ: с задержкой
     * в 10 секунд лечение в бою не идёт вовсе, поэтому число пропущенных ударов
     * за стычку должно вернуться к базовым 8.2, а отыгрыш переехать в паузы между
     * ними. Перезамер не проводился.
     *
     * Ориентир по числам: 0.5 hp/сек отыгрывают один удар обычного зомби (5 hp)
     * за 10 секунд спокойствия, полный запас героя (100 hp) — за 200 секунд.
     *
     * Выбитый боец не возвращается: регенерация идёт только тем, кто ещё в строю.
     * Мёртвый герой тоже не отыгрывается — забег заканчивается в тот же шаг.
     */
    regen: {
      hpPerInterval: 1,
      intervalSeconds: 2,
      /**
       * Пауза после последнего полученного урона, игровые секунды.
       *
       * 10 — заметно больше интервала удара обычного зомби (enemies.attackInterval
       * около секунды), поэтому в стычке таймер не истекает; и заметно меньше
       * промежутка между волнами, поэтому в передышке лечение всё-таки идёт.
       * 0 отключает задержку.
       */
      delayAfterDamageSeconds: 10,
    },
    /** Стартовое стрелковое оружие (ТЗ раздел 6): ключ из CONFIG.weapons. */
    startWeapon: 'pistol',
    /** Главный герой чуть крупнее остальных. Итоговая высота = length + 2*radius. */
    heroCapsule: { radius: 0.36, length: 1.4 },
    allyCapsule: { radius: 0.3, length: 1.2 },
    colors: {
      hero: 0x4a7fd4,
      ally: 0xd8d2c4,
    },
    /**
     * Предел движения отряда в процентах от полуширины дороги.
     * 0% = центр дороги, 100% = центр отряда ровно на кромке асфальта.
     * При 90% и дороге 10 units отряд ходит в ±4.5.
     * Ориентиры: 92.8% — край капсулы вплотную к кромке (радиус 0.36 не свисает);
     * 100% — половина капсулы висит над обочиной.
     */
    travelLimitPercent: 90,
    /**
     * Сглаживание следования за пальцем (доля остатка за шаг логики).
     * Управление абсолютное — отряд встаёт под палец, поэтому следование резкое.
     * Лерп нужен, чтобы гасить дрожание пальца; 1 = буквально мгновенно.
     */
    followLerp: 0.5,

    /*
     * Множители от мета-прокачки (ТЗ раздел 11). Пишутся MetaProgress ЦЕЛИКОМ,
     * абсолютным значением — не приращением. Поэтому повторное применение
     * прогресса ничего не накапливает, и хранить снимок базовых чисел не нужно.
     *
     * Читаются в трёх местах: аксессоры оружия (weapons.ts), получение урона
     * отрядом (squad.ts), начисление опыта (run.ts). Генерации мира не касаются.
     *
     * НАБОРЫ РАЗДЕЛЕНЫ ПО ТИПУ СТРЕЛКА. Главный герой и доп. стрелки качаются
     * независимыми ветками (meta.upgrades.hero* и meta.upgrades.ally*), поэтому
     * и множители у них свои. Раньше набор был один на весь отряд, и «прокачать
     * толпу» было невозможно в принципе: любая покупка одинаково усиливала и
     * героя, и всех союзников.
     *
     * Форма объектов у обоих наборов ОДИНАКОВАЯ: weapons.ts выбирает нужный
     * тернарником по типу стрелка, и разошедшиеся поля сломали бы этот выбор.
     */

    /** Множители главного героя. */
    heroMultipliers: {
      damageMultiplier: 1,
      fireRateMultiplier: 1,
      rangeMultiplier: 1,
      damageTakenMultiplier: 1,
    },

    /**
     * Множители доп. стрелка. Применяются к КАЖДОМУ союзнику по отдельности,
     * поверх formation.allyDamageFactor: тот задаёт долю от героя, а этот —
     * купленное усиление самой доли.
     */
    allyMultipliers: {
      damageMultiplier: 1,
      fireRateMultiplier: 1,
      rangeMultiplier: 1,
      damageTakenMultiplier: 1,
    },

    /** Опыт за забег — величина общая, к типу стрелка не относится. */
    expMultiplier: 1,
  },

  /** Построение «тупым клином» (ТЗ раздел 5). Реализация — слой 5. */
  formation: {
    /**
     * Сколько СОЮЗНИКОВ в каждом ряду за героем. Герой стоит в первом ряду один,
     * поэтому полный шаблон — 1 / 4 / 5 / 6 / 7.
     *
     * Длина массива и есть визуальный потолок: всего рисуется 4+5+6+7 = 22
     * союзника. Сейчас он совпадает с maxShooters (23 считая героя), то есть
     * виден весь отряд; механика невидимых бойцов сохранена на случай, если
     * предел снова поднимут.
     *
     * ЧЁТ-НЕЧЁТ ЗДЕСЬ НЕ СЛУЧАЕН. Ряд из N бойцов центрируется по своей
     * номинальной ширине, поэтому ряд чётной длины даёт полуцелые смещения
     * (±0.5, ±1.5 шага), а нечётной — целые (0, ±1, ±2). Чередование 4-5-6-7
     * (чёт-нечет-чёт-нечет) само выстраивает шахматку: каждый следующий ряд
     * сдвинут на полшага относительно предыдущего, и капсулы смотрят в промежутки,
     * а не в затылок. Если поменять шаблон на два чётных ряда подряд — шахматка
     * в этом месте пропадёт.
     */
    rowSizes: [4, 5, 6, 7],
    /**
     * Шаг между бойцами в ряду. 0.72 при радиусе союзника 0.30: между капсулами
     * остаётся 0.12 units, то есть строй плотный, но не слипшийся. Самый широкий
     * ряд (7 бойцов) занимает 6×0.72 = 4.32 units — влезает и в дорогу (10), и в
     * предел хода отряда.
     */
    spacingX: 0.72,
    /**
     * Шаг между рядами. Зажат с двух сторон:
     *
     *   снизу  0.55 — при шахматке ряды могут стоять ТЕСНЕЕ суммы диаметров:
     *                 соседи смещены на полшага (0.36), поэтому нужное расстояние
     *                 между центрами 0.6 даёт всего sqrt(0.6² − 0.36²) = 0.48 по z.
     *                 Ограничение жёстче задаёт герой: его радиус 0.36, и до
     *                 ближайшего бойца второго ряда нужно 0.66 — при dz = 0.6 и
     *                 боковом сдвиге 0.36 выходит 0.70, запас есть.
     *   сверху 0.83 — за героем 4.16 units до нижней кромки кадра (измерено
     *                 проекцией), а рядов союзников теперь 4, не 5.
     *
     * 0.6 берёт запас по обеим границам: строй уходит назад на 4×0.6 = 2.4 units,
     * то есть занимает меньше половины доступной глубины. Прежние 0.7 при шаге по
     * x 1.2 давали разреженную решётку без шахматки.
     */
    spacingZ: 0.6,
    /**
     * Жёсткий предел размера отряда, СЧИТАЯ ГЕРОЯ. 23 = герой + 22 союзника.
     *
     * Ограничение общее для всех источников бойцов: и бочки, и обе стены ворот
     * идут через Squad.addShooters, поэтому обрезка сделана там одна на всех.
     * Бонус сверх предела просто теряется — ворота при этом всё равно исчезают,
     * иначе они бы висели на дороге и мешали.
     *
     * ЧИСЛО СОВПАДАЕТ С ВИЗУАЛЬНЫМ ПОТОЛКОМ, и это не случайность: rowSizes даёт
     * ровно 4+5+6+7 = 22 видимых союзника, то есть при 23 на дороге виден ВЕСЬ
     * отряд, а «невидимых» бойцов не бывает никогда. Прежние 50 оставляли за
     * потолком до 27 стрелков, чьи выстрелы считались без капсул: размер отряда
     * рос, а картинка нет.
     *
     * Следствия, которые идут за этим числом:
     *   — предел ускорения потока зомби от размера отряда (enemies.spawn.squadScale)
     *     стал 1.66× вместо 2.5×: сложность растёт за силой игрока и упирается там же;
     *   — потолок огня отряда упал больше чем вдвое, поэтому запас пула пуль
     *     (weapons.bullet.poolSize) стал избыточным — см. расчёт там;
     *   — bulletsPerHiddenShooter ниже перестал на что-либо влиять.
     */
    maxShooters: 23,
    /**
     * TODO (раздел 15): рост снарядов после потолка — линейно, +1 пуля на
     * «невидимого» бойца.
     *
     * ПРИ maxShooters = 23 НЕ РАБОТАЕТ: предел отряда равен визуальному потолку,
     * невидимых бойцов не появляется. Механика оставлена — она понадобится, если
     * предел снова поднимут выше суммы rowSizes.
     */
    bulletsPerHiddenShooter: 1,
    /**
     * Множитель урона СОЮЗНИКА относительно героя. Герой всегда бьёт на 1.0,
     * каждый дополнительный стрелок — на 0.3 от того же ствола.
     *
     * Применяется к урону снаряда, а не к темпу огня: пуль в кадре столько же,
     * читаемость «отряд поливает дорогу» сохраняется, но 23 бойца дают уже не
     * 23×, а 1 + 22×0.3 = 7.6× урона одиночного героя.
     *
     * Это БАЗОВОЕ соотношение, до прокачки. Поверх него ложится множитель урона
     * союзника (player.allyMultipliers.damageMultiplier) — он сидит в weaponDamage
     * выше по потоку и умножается на этот коэффициент. С полностью прокачанной
     * веткой стрелков (×3) доля союзника доходит до 0.9 от базового урона героя,
     * а полный отряд — до 1 + 22×0.9 = 20.8× (если ветку героя не трогать).
     *
     * Множитель един для всех стволов, включая особые: у гранатомёта он так же
     * режет урон снаряда, а значит и его взрыв.
     */
    allyDamageFactor: 0.3,
  },
  /**
   * Оружие (ТЗ разделы 6 и 12).
   * range в units, а не в % экрана: не зависит от соотношения сторон телефона.
   *
   * ОТКУДА 14. В ТЗ дальность стрелкового задана как «2/3 высоты экрана».
   * Считать её как 2/3 глубины мира (43.3 units до верхней кромки → 28) НЕЛЬЗЯ:
   * перспектива сжимает дальние планы, и глубина в экранную высоту переходит
   * нелинейно. Измерено проекцией через игровую камеру: при range 28 линия
   * гашения ложится на 88% высоты кадра, а на 2/3 её кладёт range 14.3 → 14.
   * Это же даёт механику из ТЗ: «до дальних бочек пули не долетают, надо подпускать».
   *
   * ПОЧЕМУ 16.1, А НЕ 14. Базовая дальность поднята на 15% по просьбе. Это ЕДИНАЯ
   * дальность всего стрелкового оружия, а не только стартового пистолета: у
   * пистолета, мини-автомата, автомата и пулемёта она одна и та же, и поднять её
   * лишь у пистолета значило бы, что подобранный из бочки автомат стреляет БЛИЖЕ.
   * Огнемёт сохраняет свою половину (8.05): его короткая дистанция задана
   * относительно обычной, а не абсолютным числом.
   *
   * Линия гашения при 16.1 ложится на 70.2% высоты кадра вместо 66.0% — то есть
   * «2/3 экрана» из ТЗ превратились чуть больше чем в 0.7 экрана. Измерено
   * проекцией по траектории пули: точка (y = bullet.muzzleY, z = muzzleOffsetZ −
   * range), доля отсчитывается от низа кадра. Отряд для сравнения на 21.5%.
   *
   * С ПОЛНОЙ ПРОКАЧКОЙ ДАЛЬНОСТИ (×2, meta.upgrades.range) линия уезжает на 93.2%
   * высоты кадра — ЗАМЕРЕНО той же проекцией (при прежнем потолке ×1.5 было 83.3%).
   * То есть предельно прокачанный отряд достаёт почти до верхней кромки экрана;
   * зомби всё ещё рождаются дальше (z = −46 против линии гашения на z = −32.8),
   * поэтому «стрельбы по точке спавна» не возникает.
   */
  weapons: {
    /*
     * ЛЕСТНИЦА DPS СТРЕЛКОВОГО ОРУЖИЯ. Каждая следующая ступень прогрессии даёт
     * примерно в 1.5 раза больше урона в секунду, чем предыдущая, и это задаётся
     * УРОНОМ при заданном темпе огня, а не темпом: темп остался прежним (кроме
     * пистолета, ему подняли на 10%), потому что именно он определяет, как ствол
     * выглядит и звучит — пулемёт обязан поливать, пистолет пощёлкивать.
     *
     * Раньше урон у всего стрелкового был 1, и лестница шла по темпу огня: 2 → 5
     * → 7 → 15 выстрелов в секунду, то есть ×2.5, ×1.4, ×2.14 — от пистолета к
     * пулемёту 7.5 раз. Теперь от пистолета к пулемёту 3.4 раза, то есть верхние
     * стволы ослаблены (пулемёт вдвое), а нижние почти не тронуты.
     *
     *   ствол          выстр/с   урон    DPS    к предыдущему
     *   пистолет         2.2      1      2.2        —
     *   мини-автомат     5        0.67   3.35     ×1.52
     *   автомат          7        0.71   4.97     ×1.48
     *   пулемёт         15        0.5    7.5      ×1.51
     *
     * Урон округлён до двух знаков, поэтому шаг не ровно 1.5, а 1.48…1.52.
     * Округляли ВВЕРХ от точных значений (0.66, 0.707, 0.495) ради целого числа
     * попаданий по обычному зомби — тогда у него было 2 hp и выходило 3/3/4.
     * Сейчас у зомби 1 hp (enemies.normal.hp), и от округления зависит уже не
     * счёт попаданий, а только точность отношений.
     *
     * ЛЕСТНИЦА ЕСТЬ ПО УРОНУ, НО НЕ ПО УБИЙСТВАМ ОБЫЧНОГО ЗОМБИ. При 1 hp любой
     * стрелковый урон меньше единицы тратит остаток впустую: мини-автомату и
     * автомату нужно 2 попадания, пулемёту тоже 2, огнемёту 3. Кратность
     * убийств выходит 2.2 / 2.5 / 3.5 / 7.5 в секунду вместо расчётных 2.2 /
     * 3.35 / 4.97 / 7.5 — мини-автомат обгоняет пистолет всего в 1.14 раза,
     * зато пулемёт автомат в 2.14. Полностью лестница доходит только по толстым
     * целям (крупный зомби, босс, бочки). Подробнее — в enemies.normal.hp.
     *
     * ЗАМЕРЕНО (одинокий герой, цель в прицеле, 20 секунд шагами 1/60, счётчик на
     * bullets.spawn): 2.2, 5.0, 7.05 и 15.0 выстрелов в секунду, то есть 2.2,
     * 3.35, 5.0 и 7.5 урона в секунду — отношения 1.52, 1.49, 1.50. Попаданий по
     * обычному зомби — ровно 2/3/3/4, проверено прямым вызовом воронки урона.
     * Огнемёт даёт замеренные 25 выстрелов в секунду (9 DPS), гранатомёт — 0.7
     * (на 20-секундном окне это 14 выстрелов вместо 13.3: первый уходит сразу).
     *
     * ЗАМЕРЕНО НА ЗАБЕГЕ (авто-игрок наводится на ближайшего зомби, 8 прогонов до
     * смерти героя): время выживания 45.4 → 48.7 с (σ 1.7 и 2.6), убитых 34.8 →
     * 39.5 (+13.5%). Забег меряет ТОЛЬКО пистолет: авто-игрок гоняется за зомби и
     * не подбирает бочки, так что ствол за весь прогон не меняется — то есть это
     * замер ускорения пистолета на 10%, а не всей лестницы.
     */
    pistol: { fireRate: 2.2, damage: 1, range: 16.1 },
    miniSmg: { fireRate: 5, damage: 0.67, range: 16.1 },
    rifle: { fireRate: 7, damage: 0.71, range: 16.1 },
    machineGun: { fireRate: 15, damage: 0.5, range: 16.1 },
    /**
     * Особое: короткая дальность (1/2 обычной), высокий поток.
     *
     * Темп прежний, 25/сек — он и есть характер ствола, сплошная струя огня.
     * Задан DPS 9, отсюда урон 0.36 (25 × 0.36 = 9 ровно). Это ×1.2 к пулемёту
     * (7.5), то есть огнемёт стоит на ступень выше вершины стрелковой лестницы,
     * а не в разы над ней. Раньше при уроне 1 он давал 25 DPS — втрое больше
     * пулемёта; сплошной поток пламени скрывал, насколько это много.
     *
     * ПОПАДАНИЙ ПО ОБЫЧНОМУ ЗОМБИ (1 hp) ТРИ: 0.36 × 3 = 1.08, двумя не хватает
     * заметно (0.72 из 1), так что округление здесь ни на что не влияет. При
     * 25/сек три языка пламени уходят за 0.12 секунды — на глаз зомби всё так же
     * сгорает сразу, как вошёл в зону. Убийств в секунду выходит 8.3, то есть по
     * толпе огнемёт всё ещё быстрее пулемёта (7.5), хотя по урону они теперь
     * рядом (9 против 7.5).
     *
     * ЗАМЕРЕНО (неуязвимый одинокий герой с огнемётом, 60 секунд, среднее из 3
     * прогонов): убитых 73.7 при уроне 1 и 71.7 при 0.36, на дороге осталось 22.3
     * и 23.3. То есть по ПОТОКУ ЗОМБИ снижение DPS втрое почти не видно: носитель
     * один, и его ограничивает не урон, а подход целей в короткую зону. Разница
     * ложится на толстые цели — по боссу, крупному зомби и бочкам огнемёт теперь
     * бьёт в 2.8 раза слабее.
     *
     * ЗАМЕРЕНО ТАМ ЖЕ: 25 выстрелов в секунду, ровно 9 урона в секунду. Оба замера
     * сделаны, когда у обычного зомби было 2 hp и огнемёту требовалось 6
     * попаданий; сейчас у зомби 1 hp и попаданий три, так что к убийствам в
     * секунду те 71.7/73.7 больше не относятся — темп и DPS не изменились.
     */
    flamethrower: { fireRate: 25, damage: 0.36, range: 8.05 },
    /**
     * Особое: TODO (раздел 15) — дальность в ТЗ не указана, взяли как у стрелкового.
     *
     * Скорострельность уменьшена в 1.5 раза (1 → 0.6667, то есть выстрел раз в
     * 1.5 секунды), урон во столько же увеличен (10 → 15). DPS не изменился и
     * равен 10, изменился РИСУНОК: реже, но крупнее. Половина запаса крупного
     * зомби (hp 30) уходит с одного снаряда вместо трети. Дробное число, а не
     * 0.67, взято ради ровных 1.5 секунды между выстрелами — при 0.67 интервал
     * уехал бы на 1.493 и «раз в полторы секунды» перестало бы быть правдой.
     *
     * ПРОТИВ ТОЛПЫ ЭТО ЧИСТОЕ ОСЛАБЛЕНИЕ, И ВОТ ПОЧЕМУ. Взрыва (раздел 15) в коде
     * пока нет — граната это обычный снаряд, просто дорогой. По обычному зомби
     * (1 hp) избыток урона пропадает: и 10, и 15 убивают ровно одного, поэтому
     * темп выстрелов и есть темп убийств. ЗАМЕРЕНО (неуязвимый одинокий герой, 60
     * секунд, среднее из 3 прогонов): 37.7 убитых до правки и 20.0 после — падение
     * ровно в 1.5 раза, как и темп. Равный DPS сохраняется только против
     * одиночных толстых целей: босса, крупного зомби, бочек. Когда взрыв появится,
     * замер надо повторить — он и должен вернуть гранатомёту толпу.
     */
    grenadeLauncher: { fireRate: 0.6667, damage: 15, range: 16.1 },
    /**
     * Порядок прогрессии стрелкового оружия при подборе из бочки.
     * Заодно определяет, что считать ОСОБЫМ оружием: всё, чего нет в этом
     * списке. Отдельный флаг на бойце не нужен — признак выводится из ствола.
     */
    progression: ['pistol', 'miniSmg', 'rifle', 'machineGun'],
    /** Особое оружие: выдаётся одному бойцу и заменяет его ствол целиком. */
    special: ['flamethrower', 'grenadeLauncher'],
    bullet: {
      /**
       * Скорость пули, units/сек. Связана с дальностью через темп огня:
       * интервал между пулями = speed / fireRate, и он должен быть МЕНЬШЕ range,
       * иначе следующая пуля рождается уже после гашения предыдущей — на экране
       * то одна пуля, то ни одной, и линию гашения не разглядеть.
       * Правило соблюдается начиная с мини-автомата: 48 / 5 = 9.6 < 16.1.
       * У пистолета (48 / 2.2 = 21.8 > 16.1) в воздухе и правда не больше одной
       * пули — но это ЧИТАЕТСЯ как «пистолет», а не как сбой, поэтому скорость
       * под него не занижалась. Если поднимать дальность — можно поднимать и
       * скорость.
       */
      speed: 48,
      radius: 0.05,
      length: 0.7,
      color: 0xfff6bc,
      /**
       * Вид снаряда по стволу. Нужен, чтобы редкую гранату на 15 hp можно было
       * отличить от потока пуль на 0.5…1 hp, а короткий факел огнемёта — от них
       * обоих.
       * Ключ default применяется ко всему стрелковому оружию.
       */
      styles: {
        default: { color: 0xfff6bc, radiusScale: 1, lengthScale: 1 },
        /**
         * Короткие толстые языки пламени: 25/сек на половинной дальности.
         *
         * spread 4 — язык РАСШИРЯЕТСЯ вчетверо к концу своей дальности: радиус
         * идёт от 0.05 × 1.7 = 0.085 у дула до 0.34 на 8.05 units, то есть
         * диаметр на срезе 0.68 против 0.6 у капсулы обычного зомби. Поток из
         * 25 снарядов в секунду складывается в конус, хотя каждый снаряд
         * по-прежнему летит по прямой.
         *
         * pierce — цель пламя не гасит: урон достаётся ВСЕМ, кого снаряд
         * пересёк на своём пути (стену ворот он всё-таки не проходит, см.
         * Game.tryHitAnything). Из пробивания следует и хитбокс: пробивающий
         * снаряд бьёт по своей РЕАЛЬНОЙ ширине, а не по базовому радиусу пули,
         * иначе расширение было бы чисто декоративным. Обычные снаряды (в том
         * числе крупная граната, radiusScale 2.6) остались на базовом радиусе:
         * на нём сделаны все замеры стрелкового баланса.
         */
        flamethrower: {
          color: 0xff5a1f,
          radiusScale: 1.7,
          lengthScale: 0.45,
          spread: 4,
          pierce: true,
        },
        /** Крупный редкий снаряд. */
        grenadeLauncher: { color: 0xb0a668, radiusScale: 2.6, lengthScale: 1.3 },
      },
      /** Высота ствола: грудь капсулы героя (её полная высота 2.12). */
      muzzleY: 1.4,
      /** Пуля рождается перед стрелком, а не внутри капсулы. */
      muzzleOffsetZ: -0.6,
      /**
       * Размер пула. Худший случай считается точно, а не с запасом, и берётся при
       * ПОЛНОЙ прокачке — она поднимает и темп огня, и дальность (то есть время
       * жизни пули): formation.maxShooters ограничивает отряд 23 стрелками,
       * пулемёт даёт 15 × 3 = 45 выстрелов в секунду с каждого, пуля живёт
       * range/speed = 16.1 × 2 / 48 = 0.67 с. Итого 23 × 45 × 0.67 ≈ 694 пули в
       * воздухе — пул держит с запасом. Переполнение не падает, но теряет выстрелы.
       * (Без прокачки то же считается как 23 × 15 × 0.34 ≈ 116.)
       *
       * Запас стал избыточным (при пределе 50 расчёт давал 503), но пул не урезан
       * намеренно: 1200 инстансов ничего заметного не стоят, а поднимать
       * maxShooters обратно проще, когда пуля точно найдёт слот. Особое оружие
       * считать не нужно — огнемёт при 25/сек живёт вдвое короче, и достаётся он
       * ОДНОМУ бойцу, а не всему отряду: даже с полной прокачкой темпа это
       * 25 × 3 × 0.34 ≈ 25 пуль в воздухе.
       */
      poolSize: 1200,
    },
  },

  /** Зомби (ТЗ раздел 9). Реализация — слои 3 и 9. */
  enemies: {
    normal: {
      /**
       * 1 hp — обычный зомби падает от ОДНОГО попадания пистолета (урон 1).
       *
       * Было 2 hp; до того, ещё раньше, — 1. Возврат к единице, а плотность потока
       * при этом не трогали: интервалы в spawn.ramp как стояли, так и стоят, так
       * что это прямое облегчение, а не обмен. Смысл ровно тот, ради которого
       * единица бралась в первый раз: каждая пуля видимо убивает.
       *
       * СЧЁТ ПОПАДАНИЙ ВСЁ РАВНО РАЗНЫЙ У РАЗНЫХ СТВОЛОВ, потому что урон
       * стрелкового оружия давно не общая единица (см. лестницу DPS в weapons):
       *   пистолет 1        — 1 попадание
       *   мини-автомат 0.67 — 2 (0.67 не добивает, 1.34 добивает)
       *   автомат 0.71      — 2
       *   пулемёт 0.5       — 2 (ровно 1.0 со второго, hp уходит в ноль)
       *   огнемёт 0.36      — 3
       *   гранатомёт 15     — 1
       *
       * ОТСЮДА ГЛАВНОЕ ПОСЛЕДСТВИЕ ЭТОЙ ЕДИНИЦЫ: против обычного зомби лестница
       * DPS «×1.5 за ступень» не работает — работает лестница ТЕМП ДЕЛИТЬ НА ЧИСЛО
       * ПОПАДАНИЙ, а она ступенчатая: 2.2 убийства в секунду у пистолета, 2.5 у
       * мини-автомата (всего ×1.14, а не ×1.5 — половина урона пропадает в
       * перебой), 3.5 у автомата, 7.5 у пулемёта (×2.14 — скачок), 8.3 у огнемёта.
       * Расчётный DPS полностью доходит только по ТОЛСТЫМ целям: крупный зомби,
       * босс, бочки. Если захочется вернуть ровный шаг и по толпе — либо поднять
       * hp обратно до 2, либо задавать урон стволов от него кратно.
       *
       * Полоски HP над обычными зомби при этом НЕ исчезли, хотя при прошлой
       * единице их не было никогда: тогда весь стрелковый урон равнялся 1 и зомби
       * умирал от первого попадания, а теперь мини-автомат, автомат, пулемёт и
       * огнемёт оставляют его живым — ui.hpBar показывает остаток 3 секунды.
       *
       * ЗАМЕРЕНО НА ТЕКУЩИХ ЧИСЛАХ (авто-игрок наводится на ближайшего зомби,
       * забег до смерти героя, 8 прогонов; ствол весь забег пистолет — бочки
       * авто-игрок не подбирает):
       *                время выживания   убито      на дороге к концу
       *   2 hp           51.4 ± 4.0 с    44.0 ± 7.9   19.8 ± 5.2
       *   1 hp           63.3 ± 0.9 с    77.1 ± 3.6   36.5 ± 2.2
       * Забег стал длиннее на 23%, убийств больше на 75%. Зомби на дороге к концу
       * при этом БОЛЬШЕ, а не меньше, и это не противоречие: спавн разгоняется по
       * часам забега, а забег дольше — к 63-й секунде поток гуще, чем к 51-й.
       *
       * Прежний замер той же пары (68/27 против 47/48 за 60 секунд неуязвимым
       * героем) снят при пистолете 2/сек и уроне 1 у всего стрелкового — к
       * нынешним числам он относится только качественно.
       *
       * ПОРОГ, О КОТОРОМ ЛЕГКО ЗАБЫТЬ. Прокачка урона идёт до ×3
       * (meta.upgrades.damage: 50 уровней по 4%, множитель складывается —
       * 1 + 0.04 × L, см. Meta.multiplier), и счёт попаданий сходит ступеньками,
       * у каждого ствола на своём уровне:
       *   пистолет (1)        — 1 попадание с самого начала, прокачка не нужна;
       *   мини-автомат (0.67) — 2 → 1 на 13-м уровне (×1.52);
       *   автомат (0.71)      — 2 → 1 на 11-м (×1.44);
       *   пулемёт (0.5)       — 2 → 1 на 25-м (×2);
       *   огнемёт (0.36)      — 3 → 2 на 10-м (×1.4), → 1 на 45-м (×2.8).
       * Между ступеньками прокачка урона против ОБЫЧНОГО зомби не даёт ничего
       * (против крупного, босса и бочек она работает непрерывно). При 2 hp
       * ступенек было вдвое больше и стояли они выше.
       */
      hp: 1,
      damagePerHit: 5,
      capsule: { radius: 0.3, length: 1.2 },
      color: 0x6f8f5a,
      /**
       * СОПРОТИВЛЕНИЕ УРОНУ — множитель ко ВХОДЯЩЕМУ урону. Это канонический
       * комментарий, у остальных целей (enemies.big, boss, barrels) стоит ссылка
       * сюда.
       *
       * 1 — сопротивления нет, урон проходит как есть. Значение 0.5 означало бы
       * «получает вдвое меньше», то есть вдвое больше эффективного HP; 2 —
       * уязвимость. Диапазон не ограничен, но 0 делает цель неубиваемой.
       *
       * ЖИВЁТ В ВОРОНКЕ УРОНА (EnemyPool.applyDamage и такие же методы у босса и
       * бочек), а не в местах попадания. Поэтому его одинаково учитывают и пули,
       * и взрывы мин, и любой новый источник урона, подключённый через воронку, —
       * менять места попадания не нужно.
       *
       * ПОЛЕ ЕСТЬ ДАЖЕ ТАМ, ГДЕ ОНО РАВНО 1, и убирать его не надо: воронка
       * читает его безусловно, и отсутствующее поле даёт `damage * undefined`,
       * то есть NaN. HP становится NaN, проверка `hp > 0` при NaN ложна — и цель
       * умирает от первого же попадания. Ровно так и было: поля пропали из
       * конфига, а чтения остались, и от одной пули умирали и зомби, и босс.
       */
      damageResistance: 1,
    },
    big: {
      hp: 30,
      damagePerHit: 25,
      capsule: { radius: 0.5, length: 2.0 },
      color: 0x8a6a3f,
      /**
       * Как у обычного зомби — 1, сопротивления нет: крупный отличается запасом
       * HP (30 против 1), а не прочностью на единицу урона. См. канонический
       * комментарий в enemies.normal.damageResistance.
       */
      damageResistance: 1,
    },
    /**
     * Хитбокс зомби для ПОПАДАНИЙ ПУЛЬ — множитель к радиусу капсулы.
     * Модель не меняется: 1.5 расширяет только область поражения.
     *
     * Полоса поражения обычного зомби растёт с 0.78 до 1.08 units (диаметр),
     * крупного — с 1.18 до 1.68.
     *
     * ЗАМЕРЕНО ПРИ HP ОБЫЧНОГО ЗОМБИ = 1: на выживаемость почти не влияло —
     * 40 прогонов дали разницу 1.1σ при разбросе между забегами σ ≈ 27 с. Причина
     * была в том, что одна пуля убивала зомби при любом размере хитбокса, и
     * расширение помогало только тем выстрелам, которые иначе не попали бы ни во
     * что.
     *
     * Посылка снова верна ЛИШЬ ОТЧАСТИ. HP вернулся к 1, но урон стрелкового
     * оружия больше не равен 1 у всех стволов: одной пулей убивает пистолет и
     * гранатомёт, а мини-автомату, автомату и пулемёту нужно два попадания,
     * огнемёту три (см. enemies.normal.hp). То есть «зацепить краем» осмысленно
     * для всего, кроме пистолета. Заново эффект НЕ замерялся — прежний вывод
     * «влияет слабо» на текущие числа переносить нельзя.
     *
     * На взрыв мины не влияет: там считается расстояние между центрами.
     */
    hitboxScale: 1.5,
    /** Скорость зомби сверх скорости мира. Итого 6 + 1.5 = 7.5 units/сек. */
    extraSpeed: 1.5,
    /** Линия, до которой зомби доходят и дальше не наезжают на отряд. */
    stopZ: -3.5,
    /**
     * Разброс линии остановки: каждый зомби встаёт в stopZ - random*jitter.
     * Без него толпа выстраивается идеальной стеной на одной координате.
     */
    stopLineJitter: 1.5,
    /** TODO: интервал атаки в ТЗ не задан. */
    attackInterval: 1.0,
    /**
     * Пауза перед ПЕРВЫМ ударом, с момента как зомби дошёл до линии остановки.
     *
     * Раньше первый удар прилетал в тот же кадр, в который зомби доходил, и увести
     * отряд от подошедшего было невозможно — урон уже случился. Теперь у игрока
     * есть окно на реакцию, и «дошёл» перестало означать «попал».
     *
     * Отдельное число, а не attackInterval: это окно на реакцию, и крутить его
     * нужно независимо от темпа последующих ударов (у босса ровно так же —
     * boss.attacks.firstAttackDelay).
     */
    firstAttackDelay: 1.0,
    /*
     * ОГРАНИЧЕНИЯ ДОСЯГАЕМОСТИ УДАРА ЗДЕСЬ БОЛЬШЕ НЕТ.
     *
     * Было attackReachX: 1.5 — зомби бил только стрелка не дальше 1.5 units по x,
     * иначе удар уходил в никуда. Теперь удар всегда достаётся ближайшему стрелку
     * (ТЗ раздел 5 буквально), на каком бы расстоянии тот ни стоял.
     *
     * ЧТО ЭТО МЕНЯЕТ В ИГРЕ: уводом отряда поперёк дороги больше нельзя обнулить
     * урон дошедшей толпы — можно только не давать ей дойти. Каждый зомби у линии
     * остановки теперь гарантированно снимает damagePerHit раз в attackInterval,
     * поэтому давление поздних волн выросло; заново НЕ замерялось.
     */
    /**
     * АНИМАЦИЯ АТАКИ: замах раздуванием капсулы.
     *
     * Отдельной модели с руками у зомби нет, поэтому «удар» читается масштабом:
     * вторая половина кулдауна — замах, капсула плавно растёт до peakScale, в
     * момент удара начинает сжиматься обратно и возвращается к 1 за
     * recoverSeconds. Игрок видит, кто сейчас ударит, ещё до урона — тот же
     * смысл, что у firstAttackDelay, но читаемый глазом.
     *
     * Масштаб идёт ОТ ПОДОШВЫ, а не от центра капсулы: высота инстанса
     * умножается на тот же множитель, иначе раздутый зомби уходил бы в землю.
     * Хитбокс пуль и досягаемость удара при этом НЕ меняются — иначе замах
     * менял бы баланс, а не только картинку.
     *
     * windupPortion — доля кулдауна, отведённая замаху. 0.5 = «примерно
     * половина»; выше 1 ставить бессмысленно (замах не может быть дольше
     * кулдауна), ниже 0.2 замах перестаёт успевать прочитаться на глаз.
     *
     * Полоска HP висит над НЕраздутой макушкой: она привязана к габариту вида, а
     * не к текущему масштабу. Зазор ui.hpBar.offsetY = 0.9 при peakScale 1.2
     * съедается на 0.36 units у обычного зомби и на 0.6 у крупного — полоска
     * подходит к голове ближе, но не налезает.
     */
    attackAnim: {
      peakScale: 1.2,
      windupPortion: 0.5,
      recoverSeconds: 0.1,
    },
    /** Поток зомби сверху. */
    spawn: {
      /** Плоский интервал между зомби. Работает, когда разгон выключен. */
      interval: 0.35,
      /** Разброс по ширине дороги, % от полуширины (85% → ±4.25). */
      lateralSpreadPercent: 85,
      /**
       * РАЗГОН ТЕМПА. Интервал между зомби линейно сжимается от startInterval к
       * endInterval за seconds секунд забега, дальше держится на минимуме.
       *
       * Ведёт разгон время забега, а не доля израсходованного бюджета: давление
       * растёт независимо от того, успевает игрок или нет. Привязка к бюджету
       * дала бы отстающему поблажку вместо наказания.
       *
       *   1.6 с  → 0.625 зомби/сек на старте.
       *   0.94 с → 1.06 зомби/сек на 30-й секунде (середина разгона).
       *   0.28 с → 3.6 зомби/сек в пределе.
       *
       * ПОЧЕМУ СТАРТ ИМЕННО ТАКОЙ РЕДКИЙ. Первые 20 секунд забега игрок не может
       * получить вообще ничего: мини-автомат открывается на 20-й секунде, бочки со
       * стрелками — на 30-й (run.unlocks). То есть открытие обязано быть проходимым
       * для одинокого героя с пистолетом, у которого 2.2 выстрела в секунду и
       * прицеливание, а не урон, служит узким местом.
       *
       * ЗАМЕРЕНО, сколько зомби выходит на дорогу при отряде из одного стрелка
       * (счётчик enemies.spawned, шаг 1/60):
       *                    было 0.8   стало 1.6
       *   0–10 с              13          6
       *   10–20 с             16          9
       *   20–30 с             18         10
       *   первые 30 с         47         25
       *   первые 60 с        146         94
       * Первый зомби выходит на 1.55 с вместо 0.78 с.
       *
       * ЗАМЕРЕНО (по 10 прогонов на настройку, лимит 180 с, авто-игрок наводится
       * на ближайшую бочку, иначе на ближайшего зомби):
       *                              было 0.8   стало 1.6
       *   медиана выживания            37.7 с     130.2 с
       *   медиана времени до 4 стрелков  не дошёл    45.8 с
       *   минимум HP за первые 30 с      79.5       90.5
       * При 0.8 больше чем в половине прогонов отряд НИ РАЗУ не доходил до четырёх
       * стрелков — герой умирал раньше, чем открывались бочки со стрелками.
       *
       * Дальше 1.6 поднимать бесполезно: при 2.4 результат в пределах разброса
       * (medians 130.5 против 130.2), потому что на отрезке 30–45 с скорость роста
       * отряда держит разблокировка бочек, а не плотность зомби. Зато дорога на
       * первых 20 секундах становится заметно пустой.
       *
       * ПОТОЛОК ПЛОТНОСТИ СНИЖЕН: endInterval 0.175 → 0.28, то есть предел потока
       * 5.7 → 3.6 зомби/сек, поток в 1.6 раза разряжённее на всём протяжении
       * разгона (интервал линейный, поэтому середина разгона тоже сдвинулась,
       * 0.888 → 0.94 с). Прежние 0.175 задавались в те времена, когда номера волн
       * ещё не было и разгон был ЕДИНСТВЕННЫМ источником давления; теперь на него
       * сверху множится waveSpawnRateGrowth (волна 3 — ×2.25, волна 5 — ×5.1) и
       * размер отряда (до ×1.66), и предел разгона перестал быть пределом потока.
       *
       * ЗАМЕРЕНО, сколько зомби вышло на дорогу (счётчик enemies.spawned, шаг 1/60,
       * один стрелок, неуязвимый герой, авто-наведение на ближайшего, волна 1):
       *                        было 0.175   стало 0.28
       *   первые 30 с               26           25
       *   первые 60 с               95           81
       *   весь спавн волны       95.8 с      121.3 с
       * До 30-й секунды разницы почти нет и быть не может: там поток ведёт начало
       * разгона, а не его предел. Расходятся числа во второй половине разгона и
       * после неё.
       *
       * ЗАМЕРЕНО, чистый интервал на пределе разгона (EnemyPool.spawnInterval, без
       * влияния полного пула) и время выхода всего бюджета волны:
       *                              было 0.175       стало 0.28
       *   волна 1, 1 стрелок     5.7 з/с, 52.5 с   3.6 з/с, 84.0 с
       *   волна 1, отряд 20      9.0 з/с, 33.4 с   5.6 з/с, 53.5 с
       *   волна 3, 1 стрелок    12.9 з/с, 52.5 с   8.0 з/с, 84.0 с
       *   волна 3, отряд 20     20.2 з/с, 33.4 с  12.6 з/с, 53.5 с
       *
       * ПОБОЧНЫЙ ЭФФЕКТ, о котором надо помнить: бюджет волны не изменился, а
       * выходит он медленнее, поэтому волна стала в 1.6 раза длиннее (см. таблицу),
       * и босс выходит реже. Возвращать длительность нужно waveZombieCount, а не
       * множителями — они её не меняют.
       *
       * seconds оставлен 60 — растяжка до 90 замерилась в пределах шума (125 против
       * 114.7 средних при n=10), а startInterval не тронут: вход в забег и так
       * редкий (см. замеры выше), разряжать его дальше значит показывать пустую
       * дорогу первые 20 секунд.
       *
       * Скорость мира при этом не меняется намеренно: от неё зависят тайминги
       * бочек, ворот, мин и линии остановки зомби.
       */
      ramp: {
        enabled: true,
        seconds: 60,
        startInterval: 1.6,
        endInterval: 0.28,
      },
      /**
       * Размер отряда ускоряет поток: интервал делится на
       * 1 + squadScale × (стрелков − 1).
       *
       * Сложность идёт за силой игрока — собрал втрое больше стволов, получил
       * плотнее толпу. При 0.03 отряд из 12 бойцов ускоряет спавн в 1.33 раза, а
       * полный отряд из 23 (formation.maxShooters) — в 1.66 раза, и это ПРЕДЕЛ
       * вклада отряда в плотность. Считается по ВСЕМ стрелкам, включая
       * «невидимых» за визуальным потолком, — при текущем пределе их не бывает.
       */
      squadScale: 0.03,
    },
    /** Размер пула капсул зомби. */
    poolSize: 200,
  },

  /** Бочки-бонусы (ТЗ раздел 7). Реализация — слой 4. */
  barrels: {
    size: { x: 1.2, y: 1.4, z: 1.2 },
    /**
     * Сопротивление урону: до бочки доходит 75% урона, то есть вскрыть её стоит на
     * треть дороже написанного на ней числа (1 / 0.75 = 1.33). Смысл множителя — в
     * каноническом комментарии enemies.normal.damageResistance.
     *
     * hpRanges ниже при этом НЕ пересчитаны: на бочке подписана именно прочность
     * из hpRanges, и подписанное число должно совпадать с тем, что уменьшается от
     * попаданий. Сопротивление меняет, сколько выстрелов на это уйдёт: бочка на
     * 20 hp требует не 20, а 27 попаданий стрелкового оружия.
     *
     * Порог, о котором стоит помнить: пороги трещин (crackThresholds) считаются
     * долей от исходной прочности, поэтому от сопротивления не зависят.
     */
    damageResistance: 0.75,
    /**
     * Прочность бочки зависит от того, что внутри: чем ценнее содержимое, тем
     * дороже его вскрыть. Диапазоны включительные, значение целое.
     */
    hpRanges: {
      /** По стволу, который бочка выдаст. Особое оружие — здесь же. */
      weapons: {
        miniSmg: [3, 5],
        rifle: [10, 15],
        machineGun: [20, 30],
        flamethrower: [30, 40],
        grenadeLauncher: [30, 40],
      },
      /**
       * За КАЖДОГО бойца: бочка на 3 стрелков стоит 24–60. Один бросок кубика
       * умножается на количество, поэтому итог честно лежит в 8N…20N.
       *
       * Поднято с 3–8: рост отряда стал редкой находкой (shootersWeight ниже),
       * и найденная бочка должна стоить времени, а не вскрываться попутно. При
       * потолке в 3 бойца (content.shooterAmountRange) итог — 8…60 hp, а с
       * сопротивлением 0.75 это 11…80 попаданий стрелкового оружия. Верхняя
       * граница нарочно тяжелее пулемётной бочки (20–30): максимальная бочка со
       * стрелками — самая дорогая цель на дороге после босса.
       */
      shootersPerShooter: [8, 15],
      mine: [20, 30],
    },
    /** Порог появления трещин: 50% и 25% от изначальной прочности. */
    crackThresholds: [0.5, 0.25],
    /*
     * Урона от контакта здесь НЕТ намеренно: он задан как «в размере урона
     * большого зомби», то есть равен CONFIG.enemies.big.damagePerHit, и читается
     * оттуда напрямую в BarrelField.update. Отдельное число здесь означало бы две
     * константы, которые обязаны совпадать, — они бы разошлись при первой же
     * правке урона зомби.
     *
     * Бьёт не одного ближайшего стрелка, а всю полосу шириной хитбокса бочки:
     * катящаяся бочка проходит колонну целиком.
     */
    colors: {
      intact: 0x8a6b45,
      cracked50: 0xa06a3a,
      cracked25: 0xb04a2a,
    },
    /** Поток бочек сверху. */
    spawn: {
      /**
       * Секунд между бочками — но на темп находок это число почти не влияет: на
       * экране одновременно допустим ОДИН бонус (бочка или ворота, см.
       * entities/bonusSlot.ts), и подошедший тик ждёт, пока экран освободится.
       *
       * ЗАМЕРЕНО (600 с игрового времени, авто-игрок наводится на ближайшего
       * зомби, ворота выключены):
       *                          бонусов/мин   бонусов на дороге   занятость
       *   правило включено           6.3             0.96 (макс 1)    96%
       *   правило снято (как было)  16.6             2.5  (макс 3)    97%
       * То есть находки стали в 2.6 раза реже, но дорога пустой не стоит: бочка
       * живёт около 9 с (58 units / worldSpeed 6, расстрелянная меньше), а 3.5 с
       * успевают накопиться ещё до её ухода, и следующая выходит почти сразу.
       * Поднимать interval выше времени жизни бочки — это уже пустая дорога.
       *
       * Замеры весов содержимого (content.shootersWeight и рядом) правилу не
       * противоречат: там доли ТИПОВ среди появившихся бочек, а не частота.
       */
      interval: 3.5,
      /** Разброс по ширине дороги, % от полуширины. */
      lateralSpreadPercent: 70,
    },
    /** Содержимое бочек (ТЗ раздел 7). */
    content: {
      /**
       * Веса выпадения. Оружие, особое оружие и мины равны между собой; стрелки
       * стоят особняком (см. shootersWeight) — рост отряда сделан редкой находкой,
       * потому что он и есть главный источник снежного кома.
       *
       * ВАЖНО про любые «в N раз реже». Веса нормируются от ДОСТУПНЫХ на этой
       * секунде типов, а их количество меняется по ходу забега, поэтому деление
       * веса на N НЕ даёт частоту в N раз ниже. Замерено (по 4000 спавнов):
       *            вес 3    вес 1    вес 0.5
       *   100-я с   74.2%    50.8%    32.3%   (открыты оружие и стрелки)
       *   320-я с   50.1%    23.7%    15.5%   (открыты все четыре типа)
       * То есть вес 1 против веса 3 — это ×1.5 реже на первой минуте и ×2.1 реже
       * к шестой, а не втрое.
       */
      weaponWeight: 1,
      /**
       * ЗАМЕРЕНО (по 4000 спавнов на настройку, доля бочек со стрелками среди
       * всех появившихся; часы забега выставлялись напрямую):
       *            вес 1   вес 0.2   вес 0.15   вес 0.11
       *   35-я с    50.8%    17.0%      14.0%     10.6%   (оружие + стрелки)
       *   100-я с   49.2%    16.7%      12.5%     10.8%   (оружие + стрелки)
       *   320-я с   24.7%     5.6%       5.3%      3.8%   (все четыре типа)
       *
       * Взято 0.11, а не «вес поделить на 5»: веса нормируются от ДОСТУПНЫХ на
       * этой секунде типов, поэтому деление веса на 5 дало бы всего ×2.9…×4.4
       * (столбец 0.2). При 0.11 частота падает в 4.8 раза на отрезке 30–300 с —
       * там проходит почти весь забег (медиана выживания около 130 с) — и в 6.5
       * раза после 300-й секунды, когда открываются особое оружие и мины.
       * Одним весом ровно ×5 во всех фазах не получить: для этого нужны разные
       * веса в разные минуты.
       */
      shootersWeight: 0.2,
      /** Особое оружие — редкая находка: оно достаётся всего одному бойцу. */
      specialWeight: 1,
      /** Мины — разовая расчистка толпы. */
      mineWeight: 1,
      /**
       * Сколько бойцов даёт бочка со стрелками: не больше 3.
       *
       * Побочно задаёт и потолок прочности: она считается за каждого бойца
       * (hpRanges.shootersPerShooter), поэтому предел — 3×20 = 60 hp, то есть 80
       * попаданий с учётом сопротивления 0.75.
       */
      shooterAmountRange: [1, 3],
      /**
       * До этого числа рисуем N фигурок, свыше — столько же фигурок и «×N»
       * (ТЗ раздел 7: десяток фигурок в иконке не читается).
       *
       * При потолке в 3 бойца ветка с «×N» сейчас недостижима: 3 <= 4. Порог
       * оставлен как есть — он относится к отрисовке, а не к балансу, и снова
       * заработает, если потолок вернут.
       */
      iconFigureLimit: 4,
    },
    /**
     * Бочек одновременно. Их единицы, поэтому пул — отдельные Mesh, а не
     * InstancedMesh: нужен свой цвет на бочку под состояния трещин, и при таком
     * количестве лишние вызовы отрисовки ничего не стоят.
     */
    poolSize: 12,
    /** Высота числа прочности над землёй. */
    labelY: 2.2,
  },

  /** Ворота-множители (ТЗ раздел 8). Реализация — слой 6. */
  gates: {
    /** Поток ворот сверху. */
    spawn: {
      /** Секунд между воротами. Событие крупное, поэтому редкое. */
      interval: 9,
      /**
       * Вероятность, что подошедший тик действительно выпустит ворота.
       * Сделано шансом, а не растяжкой интервала, чтобы промежутки были неровными.
       *
       * СЕЙЧАС 0 — ворота отключены целиком, оба типа: и двойная стена типа A, и
       * колонна турникетов типа B. Единственный источник стрелков на время
       * эксперимента — бочки. Проверять шанс дешевле, чем сдвигать разблокировки:
       * unlocks.gateTypeA/gateTypeB и все размеры секций остались на месте,
       * включить обратно — это одно число здесь.
       *
       * Прежнее значение 0.5. Если возвращать вместе с требованием «в 5 раз реже»,
       * ставить 0.1: у ворот шанс от частоты не нормируется ничем, поэтому здесь
       * деление на 5 честно даёт ×5 (в отличие от весов бочек, см.
       * barrels.content.weaponWeight).
       */
      chance: 0,
      /** Доля ворот типа A; остальное — колонны турникетов. */
      typeAChance: 0.5,
    },
    /** Сколько секций и турникетов одновременно в пуле. */
    poolSize: 16,
    /** Тип A: большая стена из секций, стрельба растит значение секции. */
    typeA: {
      sectionCount: 2,
      /** Значение +1 за попадание. */
      shootIncrement: 1,
      /**
       * Кулдаун секции после засчитанного попадания, секунды. Попадания внутри
       * него игнорируются — пуля гасится о стену, но значение не растёт.
       *
       * Без него стена росла со скоростью огня отряда: 23 стрелка с пулемётом
       * дают 345 попаданий в секунду, то есть +345 к секции — вся механика
       * «дострелить стену» сводилась к одному касанию. Кулдаун превращает её в
       * «сколько СЕКУНД стена в прицеле», и темп роста перестаёт зависеть от
       * размера отряда и ствола: не больше 1/shootHitCooldown в секунду.
       *
       * Считается на каждую секцию отдельно: секции у стены разные (минус и
       * плюс), и общий таймер позволял бы одной глушить другую.
       */
      shootHitCooldown: 1,
      height: 2.6,
      thickness: 0.4,
      /**
       * Полная ширина стены. Меньше дороги (10) намеренно: по ТЗ мимо ворот
       * можно пройти, а стена во всю дорогу этого не позволяет. На 4.png справа
       * от стены тоже остаётся проезд.
       */
      width: 7,
      /** Первая секция отрицательная, вторая положительная — как на 4.png. */
      negativeRange: [-4, -1],
      positiveRange: [1, 4],
      /**
       * Цвет закреплён за секцией, а не за знаком: по ТЗ секции «красная и синяя»,
       * и красная может дострелиться до плюса, оставшись красной. Знак показывает
       * цвет подписи.
       */
      colors: [0xc0392b, 0x2c6fbb],
    },
    /** Тип B: колонна турникетов, простреливаются насквозь, значение фиксировано. */
    typeB: {
      valuePerGate: 1,
      /** Сколько турникетов в колонне. */
      countRange: [3, 5],
      /** Расстояние между турникетами в колонне. */
      spacingZ: 2.2,
      height: 1.8,
      thickness: 0.3,
      /** Узкие: занимают часть дороги, как на 6.png. */
      width: 4.5,
      color: 0x2c9e6b,
    },
    /** Высота подписи над воротами. */
    labelOffsetY: 0.7,
  },

  /** Противопехотная мина (ТЗ раздел 7). Реализация — слой 8. */
  mine: {
    /** Фиксированный урон по площади. Убийство не гарантировано. */
    damage: 20,
    /**
     * Радиус, игровые метры. Служит и радиусом взрыва, и дистанцией
     * срабатывания: ТЗ задаёт «при приближении», не уточняя расстояние, а
     * совпадение двух радиусов делает поведение предсказуемым — рвануло там,
     * где накрыло.
     */
    radius: 2,
    /** Активация через 1–2 сек после расстановки (ТЗ). Разброс на мину. */
    armDelayRange: [1, 2],
    /**
     * Расставляются примерно на середине поля перед отрядом.
     * Мины едут вместе с миром, как бочки и ворота: по фикции отряд наступает
     * на брошенное вперёд. От -18 это 3 секунды хода, из них ~1.5 уже активными.
     */
    placementZ: -18,
    /** Сколько мин ставит одна бочка. */
    count: 5,
    /** Полная ширина расстановки вокруг позиции отряда. */
    spreadX: 4,
    /** Плоская шайба на земле. */
    size: { radius: 0.42, height: 0.14 },
    colors: {
      /** Ещё не активна — тусклая. */
      idle: 0x4a4a52,
      /** Активна — заметная. */
      armed: 0xd83a2a,
    },
    poolSize: 16,
  },

  /** Боссфайт (ТЗ раздел 10). */
  boss: {
    totalHp: 1000,
    /**
     * Сопротивление урону: босс получает ПОЛОВИНУ входящего урона, то есть его
     * 1000 hp стоят 2000 урона. Смысл множителя — в каноническом комментарии
     * enemies.normal.damageResistance.
     *
     * Почему множитель, а не удвоенное totalHp: полоса босса подписана слоями
     * (layerCount 16), и число на ней читается игроком как «сколько раз пройти
     * полосу». Удвоив totalHp, пришлось бы либо удваивать слои, либо объяснять,
     * почему слой стал вдвое толще. Сопротивление же трогает только скорость
     * снятия и не касается ни полосы, ни выпадения кристаллов.
     *
     * РАСЧЁТ (не замер) длительности боссфайта: полный отряд из 23 стрелков с
     * пулемётом даёт 345 ВЫСТРЕЛОВ в секунду, но урон союзника режется втрое
     * (player.allyDamageFactor), то есть считать надо 1 + 22 × 0.3 = 7.6
     * «эффективных героев»: 7.6 × 15 × 0.5 = 57 урона в секунду, при
     * сопротивлении 0.5 доходит 28.5 — босс живёт около 35 секунд.
     *
     * Прежняя редакция этого комментария называла 6 секунд: она брала 345 уроном
     * (то есть забывала и про allyDamageFactor, и про то, что урон пулемёта уже
     * не равен 1). С уроном пулемёта 1 и учтённым allyDamageFactor вышло бы 17.5
     * секунды, так что снижение урона до 0.5 удвоило боссфайт. Прогоном ни то ни
     * другое не проверялось; проверено другое — что урон вообще доходит вдвое
     * меньшим (100 урона снимают 50 hp).
     */
    damageResistance: 0.5,
    /** Многослойная полоса: слоёв × HP слоя = totalHp. */
    layerCount: 16,
    capsule: { radius: 1.6, length: 4.0 },
    color: 0x7a4a4a,
    /**
     * Останавливается примерно на 1/3 экрана снизу (ТЗ).
     *
     * Измерено проекцией через игровую камеру по ОСНОВАНИЮ босса (он стоит в этой
     * точке; центр капсулы поднят на 3.6 units и уезжает по экрану выше):
     *   ровно 1/3 даёт z = -3.49 — но это линия остановки зомби, и босс шириной
     *   3.2 units встал бы почти вплотную к отряду, закрыв его собой;
     *   при -5 основание на 38.4%, центр на 45%, макушка на 54%, между боссом и
     *   отрядом остаётся 3.4 units. ТЗ говорит «примерно», поэтому взято -5.
     */
    stopZ: -5,
    /** Скорость выхода: медленнее зомби, чтобы появление читалось. */
    approachSpeed: 4,
    /** Цвета слоёв полосы, по кругу от последнего к первому. */
    layerColors: ['#e04a3a', '#e8902a', '#e8d92a', '#54c463', '#2aa7e8', '#8a5ce8'],
    attacks: {
      /**
       * AoE с красным телеграфом на земле — уклоняемая (ТЗ).
       *
       * Урон ОБЕИХ атак уменьшен вдвое (было 50 и 12.5): босс встречается каждую
       * волну, а его удары растут вместе с волной (run.waveDamageGrowth), поэтому
       * прежние числа к поздним волнам снимали героя (100 hp) за один-два удара.
       * Уполовинены синхронно, чтобы соотношение уклоняемого и неуклоняемого
       * урона (4:1) осталось прежним.
       */
      aoe: { damage: 25, radius: 3.5, telegraphTime: 1.2, cooldown: 5 },
      /** Удар по всем сразу — неуклоняемый (ТЗ). */
      allHit: { damage: 6.25, cooldown: 8 },
      /** Пауза после выхода до первой атаки. */
      firstAttackDelay: 1.5,
    },
    /**
     * АНИМАЦИЯ АТАКИ. Босс — тоже зомби, только гигантский, поэтому замах у него
     * ТОТ ЖЕ: амплитуда (peakScale) и время сжатия (recoverSeconds) читаются прямо
     * из enemies.attackAnim, своих копий здесь нет — иначе две константы, обязанные
     * совпадать, разошлись бы при первой правке (так же, как урон от наезда бочки
     * читается из enemies.big.damagePerHit).
     *
     * Своё здесь только ВРЕМЯ ЗАМАХА, и оно задано в секундах, а не долей кулдауна.
     * Правило зомби «половина кулдауна» на боссе не работает: у него кулдауны 5 и
     * 8 секунд, то есть замах занял бы 2.5–4 секунды. Босс раздувался бы больше
     * половины боя, а рост на кадр стал бы настолько мелким, что замах перестал бы
     * читаться как замах.
     *
     * 1.2 — не произвольное число, а РОВНО attacks.aoe.telegraphTime: раздувание
     * начинается в тот же кадр, в который на землю ложится красный круг, и круг с
     * раздуванием читаются как один сигнал. Для неуклоняемого удара по всем
     * телеграфа не было вообще — теперь замах предупреждает и о нём (уклониться
     * всё равно нельзя, так что на баланс это не влияет).
     *
     * Если удары двух типов сходятся ближе 1.2 с, замах считается по БЛИЖАЙШЕМУ:
     * иначе второй сигнал сбрасывал бы раздувание к началу на середине первого.
     *
     * Хитбокс (Boss.tryHit) и радиус AoE от раздувания НЕ зависят — считаются по
     * неизменному capsule.radius, как и у зомби: замах меняет картинку, не баланс.
     * Зазор до отряда при этом сжимается с 3.4 до 3.1 units (радиус 1.6 → 1.92),
     * то есть на пике замаха босс всё равно не дотягивается до строя.
     */
    attackAnim: { windupSeconds: 1.2 },
    /** Кольцо-телеграф AoE на земле. */
    telegraph: { color: 0xff2a2a, opacity: 0.35 },
  },

  /** Кристаллы EXP (ТЗ раздел 9). */
  exp: {
    perNormalZombie: 1,
    perBigZombie: 5,
    perBarrel: 3,
    crystalSize: 0.32,
    crystalColor: 0x4fd8e8,
    poolSize: 300,
    /** Парят над дорогой, а не лежат на ней. */
    y: 0.5,
    /**
     * Во сколько раз кристалл летит быстрее мира. Раньше собственной скорости у
     * него не было — он просто ехал вместе с дорогой на worldSpeed 6.
     *
     * Множитель, а не прибавка в units: «вдвое быстрее» остаётся вдвое быстрее и
     * после правки worldSpeed. Итого 6 × 2 = 12 units/сек, путь от линии остановки
     * зомби (z = −3.5) до сбора занимает 0.29 с вместо 0.58.
     */
    speedScale: 4,
    /**
     * Сближение с отрядом по x (доля остатка за шаг) — кристаллы «стягиваются».
     *
     * Удвоен вместе со speedScale, и это не косметика: доля берётся ЗА ШАГ, а
     * шагов на вдвое более коротком пути вдвое меньше. При прежних 0.05 кристалл
     * успевал сойтись к отряду лишь на 59% пути вместо 83%, то есть собирался бы
     * заметно сбоку. С 0.1 сходимость возвращается к прежним 84%.
     *
     * На сам факт сбора это не влияет: он происходит по достижении collectZ
     * независимо от x — только на то, как поток выглядит.
     */
    magnetLerp: 0.2,
    /** z, на котором кристалл считается собранным. */
    collectZ: 0,
    /** Оборотов в секунду — чтобы кристалл поблёскивал. */
    spinPerSecond: 0.8,
  },

  /**
   * Мета-прогрессия (ТЗ раздел 11 — проектировали сами).
   *
   * Улучшения затрагивают ТОЛЬКО характеристики игрока. Генерацию мира — бочки,
   * ворота, состав и число зомби, скорость мира — не трогают вообще: забеги
   * должны оставаться одинаково честными на любом уровне прокачки.
   *
   * Шаг уровня крошечный (1–4%), зато уровней 50: прокачка идёт частой мелкой
   * россыпью, а не редкими скачками.
   */
  meta: {
    /**
     * Версия в имени ключа: сменится формат — старое сохранение просто не найдётся.
     *
     * Разделение веток на героя и стрелков версию НЕ подняло намеренно: ключи
     * старого формата (damage, fireRate, …) читаются как есть и раскладываются
     * по обеим новым веткам, см. MetaProgress.migrateLegacy. Смена ключа стёрла бы
     * прогресс всем, кто уже играл.
     */
    storageKey: 'scroll-shooter-meta-v1',
    /**
     * Цена уровня = baseCost × costGrowth^level, то есть цены уровней и есть
     * геометрическая прогрессия — крутится ровно один знаменатель.
     *
     * 1.1853 = 1.115 × 20^(1/49): последний, 50-й уровень стоит РОВНО в 20 раз
     * дороже, чем при прежних 1.115 (проверено: 1.1853^49 / 1.115^49 = 20.004),
     * а первый уровень не изменился вовсе — он стоит baseCost при любом
     * знаменателе. Промежуточные уровни при этом дорожают не рывком в конце, а
     * равномерно: каждый следующий ровно на 18.53% дороже предыдущего.
     *
     * Во что это выливается (damage — exp, baseCost 10 — 25):
     *   уровень 1  — 10–25 EXP    (было столько же)
     *   уровень 10 — 46–115 EXP   (было 27–67)
     *   уровень 25 — 591–1478 EXP (было 136–341)
     *   уровень 50 — 41 456–103 640 EXP (было 2072–5181)
     *   вся ветка целиком — 212 100–662 807 EXP (было 16 010–50 017)
     * Арка удлинилась примерно в 13 раз по суммарной стоимости ветки — двадцатка
     * относится к ПОСЛЕДНЕМУ уровню, а не к сумме.
     */
    costGrowth: 1.1853,
    /** Сколько уровней берёт кнопка «пачкой». */
    batchSize: 10,
    /**
     * stepPercent — прибавка за уровень в процентах. Для damageTaken это СНИЖЕНИЕ:
     * множитель считается как 1 - stepPercent/100 × level.
     *
     * ПОТОЛОК ПРОКАЧКИ УДВОЕН (шаг за уровень удвоен, число уровней прежнее — 50):
     *   урон           ×2.00 → ×3.00 (прибавка +100% → +200%)
     *   скорострельность ×2.00 → ×3.00
     *   дальность      ×1.50 → ×2.00
     *   опыт           ×1.50 → ×2.00
     * Удвоен именно шаг, а не maxLevel: цена уровня растёт геометрически от номера
     * (см. costGrowth), и сотый уровень стоил бы неоплатимо — 1.1853^99. При
     * удвоении шага цена всей арки не меняется вовсе.
     *
     * damageTaken ОСТАВЛЕН на 1% (решение пользователя): множитель линейный,
     * 1 − 0.02 × 50 = 0, то есть удвоение шага дало бы на 50-м уровне полную
     * неуязвимость, а с ней и бесконечный забег — условие конца забега это смерть
     * героя. Потолок здесь по-прежнему ×0.50.
     *
     * ВЕТКИ РАЗДЕЛЕНЫ НА ГЕРОЯ И ДОП. СТРЕЛКОВ. Четыре боевые характеристики
     * существуют в двух экземплярах и качаются независимо (в меню это две
     * вкладки), опыт остался один — он относится к забегу, а не к стрелку.
     *
     * ЦЕНЫ И ШАГИ У ОБЕИХ ВЕТОК ОДИНАКОВЫЕ, и это осознанно:
     *   — прежняя единая ветка усиливала весь отряд сразу, теперь тот же эффект
     *     стоит двух покупок, то есть вдвое дороже. Сам факт разделения и есть
     *     балансировка; разводить цены сверх этого — второй нерф подряд;
     *   — что чем ценнее, зависит от момента забега, а не от ветки: каждый забег
     *     начинается одним героем (работает только ветка героя) и к концу волны
     *     доходит до 22 союзников (ветка стрелков даёт до 22 × 0.3 = 6.6
     *     «эффективных героев» урона). Выбор «сильное начало или сильный конец»
     *     и есть смысл разделения — при разных ценах он превратился бы в
     *     арифметику «что выгоднее за EXP».
     * Замерами это соотношение не проверялось; проверять его надо прогонами
     * с перекошенными в одну сторону ветками.
     */
    upgrades: {
      heroDamage: { maxLevel: 50, baseCost: 10, stepPercent: 4 },
      heroFireRate: { maxLevel: 50, baseCost: 12, stepPercent: 4 },
      heroRange: { maxLevel: 50, baseCost: 8, stepPercent: 2 },
      heroDamageTaken: { maxLevel: 50, baseCost: 14, stepPercent: 1 },

      allyDamage: { maxLevel: 50, baseCost: 10, stepPercent: 4 },
      allyFireRate: { maxLevel: 50, baseCost: 12, stepPercent: 4 },
      allyRange: { maxLevel: 50, baseCost: 8, stepPercent: 2 },
      allyDamageTaken: { maxLevel: 50, baseCost: 14, stepPercent: 1 },

      /**
       * Опыт самоусиливающийся: купленный рано, ускоряет все прочие покупки.
       * Поэтому базовая цена самая высокая — чтобы на старте выбор между
       * экономикой и выживанием был настоящим.
       */
      exp: { maxLevel: 50, baseCost: 25, stepPercent: 2 },
    },
  },

  /** Параметры забега (ТЗ раздел 15 — решено нами). */
  run: {
    /**
     * Бюджет ПЕРВОЙ волны в зомби: счётчик волны считает каждого за 1.
     *
     * 300 единиц. Число ОБЩЕЕ: состав (сколько из них крупных) задаётся отдельно и
     * зависит от номера волны — см. bigZombieShareStep. Раньше здесь стояли два
     * счётчика, 264 + 36, то есть фиксированные 88/12 из исходных 60 + 8 в ТЗ.
     * Прежние 2540 существовали ровно затем, чтобы один забег дотянул до
     * разблокировки гранатомёта на 8-й минуте; с волнами эта подпорка не нужна —
     * часы забега идут сквозь волны, и до поздних разблокировок игрок доживает за
     * несколько волн, а не за одну.
     *
     * ЗАМЕРЕНО, сколько идёт волна (чистый спавн, предел разгона): 84 с одиночкой,
     * 53 с отрядом 20, и это НЕ зависит от номера волны — количество и плотность
     * растут одним темпом (см. waveBudgetGrowth), поэтому длительность держится
     * постоянной. Прежние 53/21 относились к endInterval 0.175 и к текущему
     * значению 0.28 НЕ применимы (замер и причина — в enemies.spawn.ramp).
     *
     * ЗАМЕРЕНО с полным циклом игры: весь спавн первой волны — 121 секунда
     * одиночкой (авто-игрок, неуязвимый герой). Против «чистых» 84 разницу даёт
     * полный пул: один герой не успевает разгружать дорогу, живых упирается в
     * enemies.poolSize = 200, и поток ждёт слоты. С отрядом такого нет.
     *
     * То есть босс выходит каждые 50–120 секунд, если игрок успевает стрелять.
     * Если это слишком часто — растить нужно именно waveZombieCount, а не
     * множители: они длительность не меняют.
     */
    waveZombieCount: 300,
    /**
     * ДОЛЯ КРУПНЫХ ЗОМБИ В ВОЛНЕ, прибавка за каждую волну начиная с
     * bigZombieFromWave: волна 2 — 10%, волна 3 — 20%, волна 4 — 30% и так далее.
     * Всего единиц в волне это не меняет (waveZombieCount), меняется только
     * состав, поэтому длительность волны от доли не зависит.
     *
     * Раньше состав был ФИКСИРОВАННЫМ (264 обычных + 36 крупных, 88/12 из ТЗ) и
     * одинаковым во всех волнах: к десятой волне толпа отличалась от второй только
     * плотностью и множителями, а не тем, из кого она состоит. Линейный рост доли
     * даёт вторую ось сложности — на поздних волнах дорогу занимают уже крупные,
     * которых пистолетом не разобрать (30 hp против 1).
     *
     * Прибавка ЛИНЕЙНАЯ, а не кумулятивная: 10% за волну — это +10 процентных
     * пунктов, иначе доля росла бы как 10%, 11%, 12.1% и до заметной разницы дело
     * дошло бы волн через двадцать.
     */
    bigZombieShareStep: 0.1,
    /**
     * Потолок доли крупных. При шаге 0.1 линейный рост упирается в 100% на волне
     * 11 — то есть с одиннадцатой волны обычных зомби в бюджете не остаётся
     * вообще. Это и есть текущее значение: доля растёт, пока может.
     *
     * Единственная причина, по которой параметр существует отдельно: доля больше 1
     * бессмысленна, и обрезать её всё равно нужно. Заодно это одно число, которым
     * поздние волны разбавляются обратно обычными (0.6 — «не больше 60% крупных»),
     * если полностью крупная волна окажется непроходимой.
     */
    bigZombieShareMax: 1,
    /**
     * С какой волны в бюджет попадают крупные. До неё их доля отдаётся обычным,
     * поэтому число единиц в волне (а с ним и её длительность) от этого не
     * зависит: первая волна — те же 300, просто все обычные.
     *
     * Гейт по НОМЕРУ ВОЛНЫ, а не по секунде забега. Секунда наступала посреди
     * первой волны, и получалось ровно то, что видно в игре: сначала ~270 обычных
     * подряд, потом остаток бюджета крупных выходил толпой в 30 штук. Номер волны
     * такого разрыва создать не может — внутри волны состав бюджета не меняется, и
     * крупные с первой же секунды волны 2 идут вперемешку с обычными.
     *
     * 2, а не 3 и не 1: первая волна — знакомство, крупный зомби на 30 hp против
     * стартового пистолета читается как стена; ко второй волне игрок уже пережил
     * босса и почти наверняка поднял ствол хотя бы на ступень.
     *
     * Он же ТОЧКА ОТСЧЁТА доли: на этой волне крупных ровно один шаг
     * (bigZombieShareStep), дальше по шагу за волну. Сдвинув гейт, вы сдвигаете
     * всю лестницу долей, а не только её начало.
     */
    bigZombieFromWave: 2,
    hasBoss: true,
    /**
     * Конец забега: HP главного героя <= 0. Доп. стрелки на это не влияют.
     *
     * Босс концом забега БОЛЬШЕ НЕ ЯВЛЯЕТСЯ: после его смерти начинается
     * следующая волна, и так до смерти героя. Победы как исхода не существует —
     * счётом стал номер достигнутой волны.
     */
    endCondition: 'hero-death',
    /**
     * Во сколько раз чаще идут зомби в каждой следующей волне.
     *
     * Множитель кумулятивный: интервал спавна делится на growth^(волна − 1), то
     * есть волна 2 идёт в 1.5 раза плотнее первой, волна 3 — в 2.25, волна 5 — в
     * 5.1 раза.
     *
     * Делится именно интервал, а не умножается частота: разгон (ramp) и делитель
     * за размер отряда работают тем же способом, и все три множителя
     * перемножаются в одном месте — EnemyPool.spawnInterval.
     */
    waveSpawnRateGrowth: 1.5,
    /**
     * Во сколько раз больше зомби в каждой следующей волне. Тоже кумулятивный:
     * бюджет волны = round(count × growth^(волна − 1)).
     *
     * РАВЕН waveSpawnRateGrowth НАМЕРЕННО. Плотность и количество растут одним
     * темпом, поэтому длительность волны остаётся примерно постоянной, а густеет
     * только толпа. Если сделать его меньше плотности — каждая следующая волна
     * будет короче предыдущей (при прежних 2540 и неизменном бюджете волна 5
     * проходилась за 35 секунд против 3 минут у первой); если больше — длиннее.
     */
    waveBudgetGrowth: 1.5,
    /**
     * Во сколько раз крепче противники в каждой следующей волне. Кумулятивный,
     * как и два множителя выше: HP на спавне = базовое × growth^(волна − 1), то
     * есть волна 2 — 1.2, волна 3 — 1.44, волна 5 — 2.07.
     *
     * Действует на ВСЕХ противников одинаково: обычные и крупные зомби, босс.
     * Бочки под него НЕ попадают — это не противник, а источник бонуса, и
     * растущее HP превратило бы бонус в недоступный.
     *
     * МЕНЬШЕ, ЧЕМ ПЛОТНОСТЬ И КОЛИЧЕСТВО (1.5) НАМЕРЕННО. Те два множителя друг
     * друга компенсируют и длительность волны не меняют, а этот — чистое
     * удлинение: волну приходится прострелить в growth раз больше суммарного HP.
     * При 1.5 время волны росло бы вместе с ней и упиралось бы в предел урона
     * отряда; 1.2 даёт заметный, но отыгрываемый прокачкой рост.
     */
    waveHpGrowth: 1.2,
    /**
     * Во сколько раз больнее бьют противники в каждой следующей волне.
     * Кумулятивный, берётся на спавне: удар зомби = damagePerHit ×
     * growth^(волна − 1), обе атаки босса — так же.
     *
     * Отдельным числом от waveHpGrowth, хотя сейчас равен ему: это два разных
     * рычага. HP растит ДЛИТЕЛЬНОСТЬ волны (толпу дольше прострелить), урон —
     * СМЕРТНОСТЬ отряда, и упираются они в разное: HP в потолок урона отряда,
     * урон в запас HP героя и союзников. Разводить их придётся, как только одно
     * из двух окажется тяжелее другого.
     *
     * Наезд бочки под множитель НЕ попадает, хотя и читает enemies.big
     * .damagePerHit: бочка не противник, множитель применяется в атаке зомби, а
     * не в конфиге, поэтому число там осталось базовым.
     */
    waveDamageGrowth: 1.2,

    /**
     * РАЗБЛОКИРОВКИ: секунда забега, С КОТОРОЙ вещь МОЖЕТ появиться.
     * Это не расписание — появление остаётся случайным, разблокировка лишь
     * снимает запрет. До 20-й секунды бочек не бывает вообще: нечему выпадать.
     */
    unlocks: {
      /** Бочка с доп. стрелками. */
      barrelShooters: 30,
      /** Бочка с миной. */
      barrelMine: 300,
      /**
       * Бочка с оружием повышает ствол отряда на СЛЕДУЮЩУЮ ступень, поэтому
       * разблокировка проверяется по той ступени, которую бочка выдаст.
       * Побочно решает старую проблему: на последней ступени бочка с оружием
       * больше не выпадает впустую.
       */
      weapons: {
        miniSmg: 20,
        rifle: 60,
        machineGun: 180,
        /** Особое оружие (выдаётся одному бойцу). */
        flamethrower: 300,
        grenadeLauncher: 480,
      },
      /** Стена с секциями, значение которых растёт от стрельбы (тип A). */
      gateTypeA: 120,
      /** Колонна турникетов (тип B). */
      gateTypeB: 300,
    },
  },

  /** Отладка. */
  debug: {
    showAxes: false,
    /** Логировать FPS в консоль раз в секунду. */
    logFps: false,
  },
};
